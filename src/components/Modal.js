import React from "react";

function Modal({ url }) {
  return (
    <section className="Modal">
      <h3>Modal</h3>
      <a href={url}>haz click aqui</a>
    </section>
  );
}
export default Modal;
